# Packaging

Milestone 1 ships as an editable Python project plus the optional native version-stub build. Installer and distributable packaging are intentionally deferred until an application runtime is stable.
